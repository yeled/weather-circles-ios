source "https://rubygems.org"

# Pinned so this Mac and the CI runner agree on a fastlane version — the
# whole reason this file exists rather than `brew install fastlane`.
gem "fastlane", "~> 2.237"
